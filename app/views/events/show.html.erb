 <br> <br> <br>
 <div class="jumbotron">
   <% flash.each do |name,msg| %>
   <%= msg %>
   <% end%> 
   <%if @event.avatar.attached?%>
  <%= image_tag @event.avatar, alt: 'avatar' %>

<%else%>
  <p>=== Il n'y a pas encore d'avatar lié à cet evenement ===</p>
<%end%>
   <h1 class="display-3"><%= @event.title %></h1>
   <p class="lead"><%=@event.description %></p>
   <hr class="my-4">
   <p>Price:<%= @event.price %><br>
     Location:<%= @event.location%><br>
     Administrator:<%=@event.user.last_name %>- <%= @event.user.first_name %><br>
     Start_date:<%= @event.start_date%><br>
     <hr>
     <%if @event.user == current_user%>
       <hr>
       <hr>
     Voici tous les participant:
       <% @event.attendances.each do |l| %>
           <p><%=  l.user.last_name %></p>
     <% end %><br>
    <hr>
     <%= link_to "edit",edit_event_path,class: "btn btn-primary", style: "display: inline" %>
    <%= link_to "Delete",@event , method: "delete",class: "btn btn-primary", style: "display: inline"%>
     <% elsif @u == current_user%>
     <p> Vous participer a cette evenement</p>
     <% else%>
      <% if current_user %>
     <%= form_tag event_subsride_path do %>
     <article>
      <% if flash[:error].present? %>
      <div id="error_explanation">
        <p><%= flash[:error] %></p>
      </div>
      <% end %>
      <label class="amount">
        <span>Pour rejoindre cette evenement merci de payer:<%= @event.price.to_f/100%>£</span>
      </label>
    </article>

    <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
    data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
    data-description="A month's subscription"
    data-amount="<%= @event.price%>"
    data-currency="eur"
    data-locale="auto"></script>
    <% end %>
     <% else %>
    <span>Pour rejoindre cette evenement merci de payer:<%= @event.price.to_f/100%>£</span><hr>
    <P>S'il vous plait connecter-vous d'abord</P>
    <% end%>
    <% end%>
    <p class="lead"><br>
      <hr>
      <a class="btn btn-primary btn-lg" href="/" role="button">Back to list</a>
    </p>
  </div>
